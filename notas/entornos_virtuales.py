"""
primero se ingresa a la carpeta o modulo en la que se quiere crear el entorno virtual

luego de estar en esta carpeta, se ejecuta python3 -m venv env (env es el nombre que se le pondra al ambiente)

source env/bin/activate asi se activa el ambiente lo que va antes de bin es el nombre del ambiente

para salir el entorno virtual se ejecuta deactivate


instalar una libreria en un entorno virtual

pip3 install matplotlib


o para instalar una version en especifico


pip3 install matplotlib==3.5.0


which python3 con este comando se ven que entornos virtuales estan activos

pip3 freeze para ver que librerias tiene el entorno
"""