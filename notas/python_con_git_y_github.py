"""
primero se crea un repositorio publico en github (sin gitignore)
en la terminal se ven los archivos que hay
luego se ejecuta git init
y se pega el comando que da github git remoote add ....

para verificar el enlace se usa git remote -v


PROCESO PARA LOS COMMITS
git add * 
git commit -m "mensaje del commit"
git push origin master


creacion del gitignore para un proyecto profecional en python

usar https://www.toptal.com/developers/gitignore/ para la creacion de este

agregar windosw linux y mac sin importar en que OS se programe 

se crea el archivo, se copia lo que aparece
se crea un archivo .gitignore en VS code
se pega lo que genero la pagina
se crea un README.md sirve para documentar las instrucciones para cualquier persona que quiera usar el proyecto


EN CONSOLA

git add .
git commit -m "add files"
git push origin master

git status para ver el estatus de git


una recomendacion cuando se quieren usar repositorios es hacer forks (una alternativa a git clone)

los forks lo que hacen es no subir los cambios directamente a master si no que si se hacen cambios de manera local, estos se suben como PR y cuando se suben pedirle a la persona encargada del repo si acepta los cambios o no en la rama pirncipal

despues de esto si se puede hacer el git clone y hacer los cambios necesarios
"""